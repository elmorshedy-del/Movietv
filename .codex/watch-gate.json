{
  "version": 1,
  "project": "watch-together",
  "gate": "C",
  "title": "Railway Realtime Transport Spike",
  "baseBranch": "main",
  "stopAfterPass": true,
  "nextGate": "D",
  "allowedChangedPaths": [
    "server/http-server.ts",
    "server/node-build.ts",
    "server/watch/socket-probe.ts",
    "server/watch/socket-probe.test.ts"
  ],
  "requiredChangedPaths": [
    "server/http-server.ts",
    "server/node-build.ts",
    "server/watch/socket-probe.ts",
    "server/watch/socket-probe.test.ts"
  ],
  "requiredPresentPaths": [
    "server/http-server.ts",
    "server/watch/socket-probe.ts",
    "server/watch/socket-probe.test.ts"
  ],
  "requiredContent": [
    {
      "path": "server/http-server.ts",
      "contains": [
        "createNodeHttpServer",
        "closeHttpServer"
      ]
    },
    {
      "path": "server/watch/socket-probe.ts",
      "contains": [
        "WATCH_TOGETHER_SOCKET_PROBE",
        "Sec-WebSocket-Accept",
        "watch-probe-ready",
        "attachWatchSocketProbe"
      ]
    },
    {
      "path": "server/node-build.ts",
      "contains": [
        "createHttpServer",
        "attachWatchSocketProbe",
        "closeWatchSocketProbe"
      ]
    },
    {
      "path": "server/watch/socket-probe.test.ts",
      "contains": [
        "new WebSocket",
        "reconnect"
      ]
    }
  ],
  "requiredChecks": [
    "pnpm typecheck",
    "pnpm test",
    "pnpm build"
  ],
  "externalChecks": [
    "EXTERNAL VALIDATION PENDING until the probe survives about 30 minutes on the real Railway deployment from an iPhone on cellular with one background/foreground cycle"
  ],
  "instructions": [
    "Implement only the deployment transport spike. Do not create room state, playback timeline, movie types, chat, or player code.",
    "The probe may use a tiny native WebSocket handshake because it is deployment-only; production room realtime remains Socket.IO per the technical architecture.",
    "The probe must be disabled by default and enabled only when WATCH_TOGETHER_SOCKET_PROBE=1.",
    "Graceful shutdown must close probe sockets before closing the shared HTTP server.",
    "Do not modify IPTV/live-TV code.",
    "A task is not complete until pnpm watch:gate exits 0.",
    "After the deterministic gate passes, stop. Do not advance to Gate D in the same task."
  ]
}
