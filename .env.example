# Safe, non-secret defaults only. Put any real secrets in Railway Variables, never in this repo.
IPTV_UPSTREAM_ORIGIN=https://korazero.com

# Vite exposes only VITE_* variables to the browser. Do not put IPTV credentials in VITE_* variables.
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__
